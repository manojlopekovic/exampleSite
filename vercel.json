{
  "buildCommand": "hugo --gc --minify",
  "installCommand": "",
  "framework": "hugo",
  "builds": [
      {
          "src": "package.json",
          "use": "@vercel/static-build",
          "config": {
              "zeroConfig": true
          }
      }
  ],
  "build": {
      "env": {
          "HUGO_VERSION": "0.128.0" 
      }
  }
}